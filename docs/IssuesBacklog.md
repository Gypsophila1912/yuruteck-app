# 週刊ゆるてっく 実装Issue一覧

テンプレートは `ISSUE_TEMPLATE.md` を参照。各Issueをそのまま GitHub Issue にコピーして使えます。
ラベル案: `phase:0`〜`phase:9` / `type:setup` `type:feature` `type:infra` / `priority:high` `priority:mid` `priority:low`

---

## Phase 0: プロジェクト基盤構築

### #1 プロジェクト初期セットアップ（Next.js / TypeScript / Tailwind / shadcn-ui）

**概要**: Next.js (App Router) + TypeScript + Tailwind CSS + shadcn/ui の雛形を作成する。

**実装タスク**

- [ ] `create-next-app` でプロジェクト作成（App Router, TypeScript, Tailwind）
- [ ] shadcn/ui 初期化、基本コンポーネント（Button, Input, Dialog等）導入
- [ ] Lucide React インストール
- [ ] ディレクトリ構成を仕様書通りに整備（`components/ui`, `components/features`, `lib`, `hooks`, `types`, `schemas`, `constants`）

**完了条件**

- [ ] `pnpm dev` でトップページが表示される
- [ ] shadcn/uiのButtonコンポーネントが動作確認できる

**依存Issue**: なし
**参考資料**: 技術選定書（フロントエンド節）

---

### #2 ESLint / Prettier / Lefthook 設定

**概要**: コード品質ツールを導入し、コミット前チェックを自動化する。

**実装タスク**

- [ ] ESLint設定（Next.js推奨設定ベース）
- [ ] Prettier設定、ESLintとの競合ルール解除
- [ ] Lefthookインストール、pre-commitフックに lint / format / test を登録

**完了条件**

- [ ] 意図的にフォーマット崩れたコードをコミットしようとするとフックで弾かれる

**依存Issue**: #1

---

### #3 Drizzle ORM + TiDB Cloud 接続確認

**概要**: Drizzle ORMをセットアップし、TiDB Cloud Starterクラスタへの接続を確認する。

**実装タスク**

- [ ] TiDB Cloud Starterクラスタ作成（無料枠、クレカ不要）
- [ ] `drizzle-orm`, `drizzle-kit`, `@tidbcloud/serverless`, `@paralleldrive/cuid2` インストール
- [ ] `drizzle.config.ts` 作成（dialect: mysql, dbCredentials.url）
- [ ] 疎通確認用の最小テーブルで `drizzle-kit generate` → `drizzle-kit migrate` を試す
- [ ] `.env` / `.dev.vars` に `DATABASE_URL` を設定

**完了条件**

- [ ] ローカルからTiDBに対してSELECTクエリが実行できる
- [ ] `drizzle-kit studio` でテーブルが確認できる

**依存Issue**: #1
**参考資料**: TiDB Cloud公式Drizzleチュートリアル
**備考**: 開発中の接続はTCP接続文字列でOK。本番Worker実行時は`drizzle-orm/tidb-serverless` + `@tidbcloud/serverless`のHTTP接続に切り替える（#5で対応）。

---

### #4 Cloudflare Workers + OpenNext デプロイ雛形

**概要**: Next.jsアプリを Cloudflare Workers にデプロイできる状態を作る（Hello World段階）。

**実装タスク**

- [ ] `@opennextjs/cloudflare` インストール
- [ ] `wrangler.jsonc` 作成（`compatibility_flags: ["nodejs_compat"]`、`compatibility_date`は最新日付）
- [ ] `wrangler deploy` で `*.workers.dev` へのデプロイを確認
- [ ] カスタムドメイン設定（任意、後回し可）

**完了条件**

- [ ] デプロイ後のURLにアクセスしてトップページが表示される

**依存Issue**: #1
**参考資料**: Cloudflare公式 Next.js on Workers ガイド
**備考**: 2025年12月以降、CloudflareはPagesではなくWorkers+OpenNextを公式推奨としている。

---

### #5 本番環境でのDrizzle + TiDB serverless driver 接続

**概要**: Cloudflare Workers上で `drizzle-orm/tidb-serverless` を使ってTiDBにHTTP接続する。

**実装タスク**

- [ ] `drizzle-orm/tidb-serverless` を使った接続コードに切り替え
- [ ] Workers環境変数（Secrets）に `DATABASE_URL` を設定
- [ ] デプロイ環境で実際にSELECT/INSERTが動作することを確認

**完了条件**

- [ ] 本番Worker上のAPIエンドポイントからTiDBへの読み書きが成功する

**依存Issue**: #3, #4

---

### #6 GitHub Actions CI構築

**概要**: PR時に lint → typecheck → test → build を自動実行する。

**実装タスク**

- [ ] `.github/workflows/ci.yml` 作成
- [ ] pnpm キャッシュ設定
- [ ] 各ステップ（lint/typecheck/test/build）を順に実行するよう設定

**完了条件**

- [ ] PRを作成すると自動でCIが走り、成功/失敗が表示される

**依存Issue**: #1, #2

---

### #7 環境変数設計・初回管理者bootstrap方針の実装

**概要**: 環境変数管理の方針を固め、初回管理者を登録する仕組みを作る。

**実装タスク**

- [ ] `.env.example` 作成（DATABASE_URL, DISCORD_CLIENT_ID, DISCORD_CLIENT_SECRET, INITIAL_ADMIN_DISCORD_ID等）
- [ ] 初回ログイン時、`INITIAL_ADMIN_DISCORD_ID` と一致するユーザーのroleをADMINにするロジック実装

**完了条件**

- [ ] 環境変数に設定したDiscordIDでログインすると自動的にADMIN roleが付与される

**依存Issue**: #3, Phase1のログイン実装後

---

## Phase 1: 認証・DBスキーマ

### #8 Drizzleスキーマ実装（全テーブル）

**概要**: users / providers / user_provider_accounts / events / articles / point_logs のスキーマを実装する。

**実装タスク**

- [ ] `src/db/schema.ts` に全テーブル定義（前回合意の設計通り、`point_logs.article_id`含む）
- [ ] リレーション定義（`relations()`）
- [ ] `drizzle-kit generate` でマイグレーションファイル生成
- [ ] TiDBに適用

**完了条件**

- [ ] 全テーブルがTiDB上に作成されている
- [ ] Drizzle Studioで各テーブルが確認できる

**依存Issue**: #3

---

### #9 Provider初期データ投入

**概要**: Qiita / Zenn / 静かなインターネット / 個人ブログ / その他 をシードする。

**実装タスク**

- [ ] `src/db/seed.ts` 作成（Faker利用、Prisma-fabbricaの代わり）
- [ ] Provider初期データ投入スクリプト実行

**完了条件**

- [ ] `providers`テーブルに5件のレコードが存在する

**依存Issue**: #8

---

### #10 Discord OAuth ログイン実装

**概要**: Auth.jsを使ったDiscordログインを実装する。

**実装タスク**

- [ ] Auth.js インストール、Discord Provider設定
- [ ] Drizzle Adapter設定（Auth.js用のセッション/アカウントテーブル追加が必要か確認）
- [ ] ログイン/ログアウトボタン実装
- [ ] セッションからユーザー情報を取得するhook/utilを実装

**完了条件**

- [ ] Discordアカウントでログインでき、`users`テーブルにレコードが作成される
- [ ] ログアウトが機能する

**依存Issue**: #8
**備考**: Auth.jsのDrizzle Adapterは別途セッション管理用テーブルが必要になる場合がある。仕様書のusersテーブル設計と整合を取ること。

---

### #11 管理者判定・権限ガード実装

**概要**: role=ADMINのユーザーのみ管理画面にアクセスできるようにする。

**実装タスク**

- [ ] middleware または Server Component側でrole判定
- [ ] 管理画面配下のルートガード実装
- [ ] 非ADMINユーザーがアクセスした場合のリダイレクト/エラー表示

**完了条件**

- [ ] USER roleのユーザーは管理画面にアクセスできない
- [ ] ADMIN roleのユーザーは管理画面にアクセスできる

**依存Issue**: #7, #10

---

## Phase 2: イベント管理

### #12 管理者による手動イベント作成 + Discord案内文生成

**概要**: 管理者が部会終わりに手動でイベントを作成する。通常イベントの場合はDiscordに貼る登壇者募集の案内文を自動生成する。

**実装タスク**

- [ ] `events` テーブルに `is_regular`（通常/特別）カラム追加・マイグレーション
- [ ] イベント作成APIの実装（`POST /api/admin/events`）
  - タイトル自動採番（前回の「第◯回」+1）
  - 管理者のみアクセス可能なガード
- [ ] イベント作成フォームUI実装（`/admin/events/new`）
  - タイトル・開始日時・終了日時・種別（通常/特別）を入力
- [ ] 通常イベント作成後にDiscordへ貼る案内文テンプレートを表示（コピーボタン付き）
  - 特別イベントの場合は案内文を表示しない

**完了条件**

- [ ] 管理者が `/admin/events/new` でイベントを作成できる
- [ ] 種別「通常」でイベント作成後、Discord案内文テンプレートが画面に表示される
- [ ] 種別「特別」では案内文が表示されない
- [ ] 非管理者がAPIを叩いた場合 403 が返る

**依存Issue**: #8, #11
**備考**: Cronは使わない。`wrangler.jsonc` の `crons` 設定は空のままでよい。

---

### #13 管理者用イベント管理画面

**概要**: イベント一覧の確認・管理画面を実装する。

**実装タスク**

- [ ] イベント一覧表示（管理画面）
- [ ] イベント詳細確認（開始/終了日時・種別・作成日）

**完了条件**

- [ ] 管理者がイベント一覧を確認できる

**依存Issue**: #11, #12
**備考**: イベント新規作成フォームは #12 に含まれる。

---

## Phase 3: 記事登録

### #14 OGPスクレイピングAPI実装

**概要**: URLからOGPタグ（公開日時等）を取得するAPIを実装する。

**実装タスク**

- [ ] URLをfetchしHTMLをパース、`og:article:published_time`等を抽出するAPI Route実装
- [ ] タイムアウト・エラーハンドリング（取得失敗時はnullを返す）
- [ ] SSR対策：外部URLフェッチ時のSSRF対策（許可スキームの制限等）を検討

**完了条件**

- [ ] 対応済みサイト（Qiita/Zenn等）のURLを渡すと公開日時が取得できる
- [ ] 非対応サイトや取得失敗時にエラーではなくnullが返る

**依存Issue**: #1

---

### #15 手動URL登録フロー実装

**概要**: ユーザーがURLを入力して記事を登録するフローを実装する。

**実装タスク**

- [ ] URL入力フォーム（Zodバリデーション）
- [ ] OGP取得APIを呼び出し、公開日時を自動セット
- [ ] OGP取得失敗時は手入力フォールバックUIを表示
- [ ] 公開日時からイベントを自動判定するロジック実装
- [ ] contentType（ARTICLE/SLIDE）選択UI

**完了条件**

- [ ] URLを入力して記事登録が完了し、`articles`テーブルにレコードが作成される
- [ ] 公開日時が正しいイベントに紐付く

**依存Issue**: #14, #8

---

### #16 1ユーザー1記事の重複エラーハンドリング

**概要**: 同一イベントに既に記事登録済みの場合、UI上でわかりやすくエラーを出す。

**実装タスク**

- [ ] DB unique制約違反を検知し、ユーザー向けメッセージに変換
- [ ] 「そのイベントは既に登録済みです」等のUI表示

**完了条件**

- [ ] 同一イベントに2件目を登録しようとすると適切なエラーメッセージが表示される

**依存Issue**: #15

---

### #17 RSS取得API実装（オンデマンド）

**概要**: 記事投稿画面を開いたタイミングでRSSを取得し候補を表示する。

**実装タスク**

- [ ] `rss-parser` インストール
- [ ] ユーザーのRSS URL（`user_provider_accounts`）からフィード取得するAPI実装
- [ ] 取得失敗時のエラーレスポンス設計

**完了条件**

- [ ] RSS連携済みユーザーが記事投稿画面を開くと最新の候補記事一覧が取得できる

**依存Issue**: #8

---

### #18 RSS候補選択→登録フロー実装

**概要**: RSS候補から記事を選んで登録できるUIを実装する。

**実装タスク**

- [ ] RSS候補一覧表示コンポーネント
- [ ] 候補選択→#15の登録ロジックと合流させる
- [ ] RSS取得失敗時、エラー表示＋手動URL登録への導線を表示

**完了条件**

- [ ] RSS候補から記事を選んで登録が完了する
- [ ] RSS取得失敗時にエラーメッセージと手動登録への導線が出る

**依存Issue**: #17, #15

---

### #19 RSS連携設定画面

**概要**: マイページからRSS連携（サービス毎のURL登録）を設定できるようにする。

**実装タスク**

- [ ] RSS URL登録フォーム（Provider選択＋URL入力）
- [ ] 初回ログイン時の案内表示（「RSS連携すると記事登録が楽になります」、スキップ可能）
- [ ] 個人ブログは1ユーザー1件までのバリデーション

**完了条件**

- [ ] RSS URLを登録・編集できる
- [ ] 初回ログイン時に案内が表示され、スキップも選べる

**依存Issue**: #9, #10

---

## Phase 4: ポイント機能

### #20 記事投稿ポイント自動付与

**概要**: 記事登録時に+1ポイントを自動付与する。

**実装タスク**

- [ ] 記事登録処理内で `point_logs` に ARTICLE タイプのレコードを作成（`articleId`紐付け）

**完了条件**

- [ ] 記事登録すると自動的にポイントが+1される

**依存Issue**: #15

---

### #21 管理者用ポイント管理画面

**概要**: 管理者がポイントのログ確認・付与・編集・削除を行えるポイント管理ページを実装する。

**実装タスク**

- [ ] ポイントログ一覧表示（`/admin/points`）
- [ ] 「ポイントを操作する」ボタン → モーダルを開く
- [ ] モーダル内タブ1「付与」: イベント選択・ユーザー選択・種別・理由・ポイント数を入力して付与
- [ ] モーダル内タブ2「編集・削除」: 既存レコードの修正・削除
- [ ] MANUAL種別時は理由の入力を必須バリデーション

**完了条件**

- [ ] 管理者がポイントログを一覧で確認できる
- [ ] モーダルからポイントの付与・編集・削除ができる

**依存Issue**: #11, #8

---

### #22 マイページ：ポイント履歴表示

**概要**: マイページで自分のポイント履歴を表示する。

**実装タスク**

- [ ] `point_logs`をユーザーIDで絞り込み、種別・理由・日時を一覧表示

**完了条件**

- [ ] 自分のポイント履歴が新しい順で確認できる

**依存Issue**: #20, #21

---

## Phase 5: 記事一覧・検索

### #23 記事一覧（無限スクロール）

**概要**: ログイン不要で閲覧できる記事一覧を実装する。

**実装タスク**

- [ ] 記事一覧API（新しい順、ページネーション/カーソル対応）
- [ ] 無限スクロールUI実装（TanStack Query利用）
- [ ] 発表記事/投稿のみバッジ表示

**完了条件**

- [ ] スクロールすると追加の記事が読み込まれる
- [ ] ログインしていない状態でも一覧が閲覧できる

**依存Issue**: #8

---

### #24 記事検索・絞り込み機能

**概要**: タイトル/投稿者/サービス/イベントで記事を絞り込めるようにする。

**実装タスク**

- [ ] 検索UI実装（React Hook Form）
- [ ] 検索API実装（複数条件のAND検索）
- [ ] URLクエリパラメータとの同期（`?event=15`等、Discord投稿リンク用）

**完了条件**

- [ ] 各条件で正しく絞り込みができる
- [ ] `?event=15`のようなURLで直接絞り込み結果を表示できる

**依存Issue**: #23

---

## Phase 6: マイページ・メンバー一覧

### #25 マイページ実装

**概要**: プロフィール・ポイント・投稿記事・RSS設定をまとめたマイページを実装する。

**実装タスク**

- [ ] マイページレイアウト実装（アイコン・名前・総ポイント）
- [ ] 投稿記事一覧セクション実装
- [ ] RSS設定セクションへの導線

**完了条件**

- [ ] ログインユーザーが自分の情報を一通り確認できる

**依存Issue**: #22, #19

---

### #26 メンバー一覧実装

**概要**: サークルメンバー全体の一覧（アイコン・名前・総ポイント）を表示する。

**実装タスク**

- [ ] 全ユーザーの総ポイント集計クエリ実装
- [ ] メンバー一覧UI実装

**完了条件**

- [ ] 全メンバーの総ポイントが一覧で確認できる

**依存Issue**: #20

---

## Phase 7: 発表管理

### #27 発表者選択・ランダム発表順生成

**概要**: 今週の記事一覧から発表者を複数選択し、ランダムに発表順を生成する。

**実装タスク**

- [ ] 今週の記事一覧＋チェックボックスUI実装
- [ ] ランダム生成ボタン→`presentationOrderJson`に保存
- [ ] 再読み込みしても状態が保持されることを確認

**完了条件**

- [ ] チェックした発表者からランダムな発表順が生成され、DBに保存される
- [ ] 画面を閉じて開き直しても順番が保持される

**依存Issue**: #8, #13

---

### #28 Discord用コピー生成

**概要**: 発表順・記事投稿一覧をDiscord投稿用マークダウンとして生成する。

**実装タスク**

- [ ] マークダウン生成ロジック実装（仕様書15章のフォーマット通り）
- [ ] 記事一覧リンク（`?event=15`形式）を含める
- [ ] クリップボードコピー機能実装

**完了条件**

- [ ] ボタン一つでDiscord投稿用テキストがクリップボードにコピーされる

**依存Issue**: #27, #24

---

### #29 発表ポイント付与処理

**概要**: 部会終了後、発表者に発表ポイントを付与し`isPresenter`を確定する。

**実装タスク**

- [ ] 発表ポイント付与ボタン実装
- [ ] 付与と同時に対象記事の`isPresenter`を`true`に更新
- [ ] `point_logs`にPRESENTATIONタイプのレコード作成

**完了条件**

- [ ] ボタンを押すと発表者全員にポイントが付与され、バッジ表示が切り替わる

**依存Issue**: #27, #21

---

## Phase 8: 管理機能統合

### #30 管理画面ダッシュボード・共通レイアウト

**概要**: サイドバー形式の管理画面レイアウトと、ダッシュボードトップページを実装する。

**実装タスク**

- [ ] 管理画面共通レイアウト（サイドバー + ヘッダーバー）実装
  - サイドバー: ダッシュボード / イベント管理 / ポイント管理 / 発表管理 / メンバー管理
  - ヘッダーバー: 「+ 新規イベント作成」ボタン → モーダル
- [ ] ダッシュボードトップ（`/admin`）: 各セクションへのクイックリンクカード

**完了条件**

- [ ] サイドバーで全管理機能に遷移できる
- [ ] ヘッダーの新規作成ボタンからイベント作成モーダルが開く

**依存Issue**: #11, #12, #21, #27

---

### #34 管理者用メンバー管理画面

**概要**: 管理者が全メンバーの一覧を確認し、roleの変更等の管理ができる画面を実装する。

**実装タスク**

- [ ] 全ユーザー一覧表示（`/admin/members`）（アイコン・名前・Discord ID・role・登録日）
- [ ] role変更機能（USER ↔ ADMIN の切り替え）

**完了条件**

- [ ] 全メンバーの一覧が確認できる
- [ ] メンバーのroleを変更できる

**依存Issue**: #11, #8

---

## Phase 9: 仕上げ

### #31 レスポンシブ対応確認

**概要**: PC/スマホ両方で崩れなく表示されることを確認する。

**実装タスク**

- [ ] 主要画面をスマホ幅で確認・修正

**完了条件**

- [ ] 主要画面がスマホ幅でも崩れずに操作できる

**依存Issue**: 各画面実装完了後

---

### #32 エラーハンドリング全体レビュー

**概要**: RSS取得失敗、OGP取得失敗などのエラー表示が一貫しているか確認する。

**実装タスク**

- [ ] 各エラーケースの表示内容を横断的にレビュー

**完了条件**

- [ ] 主要なエラーケースで適切なメッセージが表示される

**依存Issue**: 全機能実装後

---

### #33 本番デプロイ・Discord OAuthリダイレクトURI設定

**概要**: 本番環境への最終デプロイと動作確認を行う。

**実装タスク**

- [ ] 本番Discord OAuth設定（リダイレクトURI登録）
- [ ] 本番環境変数設定
- [ ] 一通りの機能を本番環境で動作確認

**完了条件**

- [ ] 本番URLで一通りの機能（ログイン〜記事登録〜ポイント確認）が動作する

**依存Issue**: 全機能実装後
